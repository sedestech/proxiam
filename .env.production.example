# ═══════════════════════════════════════════
# Proxiam v3.0.0 — PRODUCTION Environment
# ═══════════════════════════════════════════
# Copier vers .env et remplacer TOUTES les valeurs
# AUCUNE valeur par défaut ne doit rester en production

# ─── Domaine & URLs ───
DOMAIN=proxiam.fr
VITE_API_URL=https://proxiam.fr
BACKEND_CORS_ORIGINS=https://proxiam.fr,https://www.proxiam.fr

# ─── PostgreSQL + PostGIS ───
# Générer : openssl rand -base64 32
POSTGRES_HOST=database
POSTGRES_PORT=5432
POSTGRES_DB=proxiam
POSTGRES_USER=proxiam
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_RANDOM_32CHARS

# ─── Redis ───
# Générer : openssl rand -base64 32
# OBLIGATOIRE en production (auth activée dans docker-compose)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=CHANGE_ME_GENERATE_RANDOM_32CHARS

# ─── Meilisearch ───
# Générer : openssl rand -base64 32
MEILI_HOST=http://meilisearch:7700
MEILI_MASTER_KEY=CHANGE_ME_GENERATE_RANDOM_32CHARS

# ─── MinIO (S3-compatible storage) ───
# Générer : openssl rand -base64 24
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=proxiam-admin
MINIO_SECRET_KEY=CHANGE_ME_GENERATE_RANDOM_32CHARS
MINIO_BUCKET=proxiam-docs

# ─── Backend ───
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# ─── IA (Anthropic Claude) ───
# Requis pour : scoring IA, analyse docs, enrichissement, veille
# Obtenir sur : https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-VOTRE_CLE_ICI

# ─── Auth (Clerk) ───
# Requis pour : login, multi-tenant, gestion utilisateurs
# Obtenir sur : https://dashboard.clerk.com/
CLERK_SECRET_KEY=sk_live_VOTRE_CLE_ICI
CLERK_PUBLISHABLE_KEY=pk_live_VOTRE_CLE_ICI
VITE_CLERK_PUBLISHABLE_KEY=pk_live_VOTRE_CLE_ICI

# ─── SMTP (pour alertes — déjà configuré sur VPS via msmtp) ───
# Pas besoin ici, msmtp gère l'envoi au niveau système
